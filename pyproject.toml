[tool.poetry]
name = "genesis_v3"
version = "0.1.0"
description = "Genesis V3 research-grade MVP"
authors = ["Genesis Team <dev@example.com>"]

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
pydantic = "^2.6"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
